<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="org.zues.dao.UserDao">

    <!--新增-->
    <insert id="save">
        INSERT INTO `user` (`name`, `age`) VALUES ( #{name}, #{age});
    </insert>

    <!--详情-->
    <select id="getUserById" resultType="org.zues.entity.UserEntity">
        select * from user where id = #{id}
    </select>

</mapper>